interface AnalyticsData {
  t: number; // Timestamp
  c: number; // CPU
  m: number; // Memory
  i: number; // Network In
  o: number; // Network Out
  d: number; // Disk Usage
}

export { AnalyticsData };
